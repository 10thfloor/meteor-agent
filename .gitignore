node_modules/
.meteor/local/
.DS_Store

# Local credentials (Slack tokens etc.) — never committed; copy settings.example.json
app/settings.json
settings.json

# Deployment credentials — copy the tracked example, never commit the copy.
deploy/galaxy.settings.json
