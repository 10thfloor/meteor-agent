{
  "name": "app",
  "private": true,
  "scripts": {
    "start": "meteor run",
    "typecheck": "tsc --noEmit",
    "release:check": "node ../scripts/check-release.mjs",
    "types": "npm run types:core && npm run types:channels",
    "types:core": "rm -rf packages/agent/types && tsc -p tsconfig.types.json",
    "types:channels": "rm -rf packages/agent-channel-slack/types packages/agent-channel-telegram/types packages/agent-channel-whatsapp/types packages/agent-channel-sms/types packages/agent-channel-email/types && tsc -p packages/agent-channel-slack/tsconfig.types.json && tsc -p packages/agent-channel-telegram/tsconfig.types.json && tsc -p packages/agent-channel-whatsapp/tsconfig.types.json && tsc -p packages/agent-channel-sms/tsconfig.types.json && tsc -p packages/agent-channel-email/tsconfig.types.json",
    "types:consumer": "tsc -p tsconfig.consumer.json",
    "types:check": "npm run types && node ../scripts/check-generated-types.mjs && npm run types:consumer",
    "test": "TEST_BROWSER_DRIVER=playwright meteor test-packages --once --port 3200 --driver-package meteortesting:mocha ./packages/agent ./packages/agent-channel-slack ./packages/agent-channel-telegram ./packages/agent-channel-whatsapp ./packages/agent-channel-sms ./packages/agent-channel-email",
    "test-app": "TEST_WATCH=1 meteor test --full-app --driver-package meteortesting:mocha",
    "test-app:once": "CONSTELLATION_OFFLINE=1 TEST_BROWSER_DRIVER=playwright meteor test --full-app --once --port 3200 --driver-package meteortesting:mocha",
    "visualize": "meteor --production --extra-packages bundle-visualizer"
  },
  "dependencies": {
    "@babel/runtime": "^7.23.5",
    "@earendil-works/pi-ai": "^0.84.2",
    "@modelcontextprotocol/sdk": "^1.30.0",
    "@swc/helpers": "^0.5.17",
    "marked": "18.0.11",
    "meteor-node-stubs": "^1.2.29",
    "typebox": "^1.3.7"
  },
  "meteor": {
    "mainModule": {
      "client": "client/main.js",
      "server": "server/main.js"
    },
    "testModule": "tests/main.js",
    "modern": true
  },
  "devDependencies": {
    "@types/chai": "^5.2.3",
    "@types/meteor": "^2.9.11",
    "@types/mocha": "^10.0.10",
    "chai": "^6.2.2",
    "playwright": "^1.62.1",
    "typescript": "^7.0.2"
  }
}
